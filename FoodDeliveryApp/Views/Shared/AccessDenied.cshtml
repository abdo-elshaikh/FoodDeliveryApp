@{
    ViewData["Title"] = "Access Denied - Food Delivery App";
    Layout = null;
}

<div class="access-denied-page min-vh-100 d-flex align-items-center justify-content-center py-5" style="background: linear-gradient(135deg, #f8d7da 0%, #f5c6cb 100%);">
    <div class="container text-center">
        <div class="bg-white rounded-4 shadow-sm p-5">
            <i class="fas fa-ban fa-4x text-danger mb-4"></i>
            <h1 class="display-5 fw-bold mb-3">Access Denied</h1>
            <p class="lead text-danger mb-4">You do not have permission to view this page.</p>
            <a href="/" class="btn btn-danger btn-lg rounded-pill px-4">
                <i class="fas fa-home me-2"></i>Go Home
            </a>
        </div>
    </div>
</div>

<style>
    .access-denied-page {
        animation: fadeIn 0.5s ease-in-out;
    }
    @@keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
</style>
