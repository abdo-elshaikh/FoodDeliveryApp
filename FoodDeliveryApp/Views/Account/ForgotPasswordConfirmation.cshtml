@{
    ViewData["Title"] = "Password Reset Sent";
    ViewData["SubTitle"] = "Check your email for a reset link.";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<div class="text-center">
    <p class="lead">If an account with the provided email exists, a password reset link has been sent.</p>
    <a href="@Url.Action("Login", "Account")" class="btn btn-primary rounded-pill px-4">Back to Login</a>
</div>