@{
    ViewData["Title"] = "Password Reset Complete";
    ViewData["SubTitle"] = "Your password has been successfully reset.";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<div class="text-center">
    <p class="lead">You can now sign in with your new password.</p>
    <a href="@Url.Action("Login", "Account")" class="btn btn-primary rounded-pill px-4">Sign In</a>
</div>
