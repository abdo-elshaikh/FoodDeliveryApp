@{
    ViewData["Title"] = "Email Confirmed";
    ViewData["SubTitle"] = "Your email has been verified.";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<div class="text-center">
    <p class="lead">Thank you for confirming your email. You can now sign in.</p>
    <a href="@Url.Action("Login", "Account")" class="btn btn-primary rounded-pill px-4">Sign In</a>
</div>